apiVersion: 1

providers:
  - name: "Enterprise KM"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
